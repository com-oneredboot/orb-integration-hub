{%- for schema in table_schemas.values() %}
{%- for operation in schema.operations if operation.type == 'Mutation' %}
  {{ operation.field }}(
    input: {{ schema.name }}{{ operation.name }}Input
  ): {{ schema.name }}Response{% if operation.response_auth_directives and operation.response_auth_directives|length > 0 %} {{ operation.response_auth_directives|join(' ') }}{% endif %}
{%- endfor %}
{%- endfor %} 