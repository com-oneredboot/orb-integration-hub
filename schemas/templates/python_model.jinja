"""
{{ table_name | to_pascal_case }} model.
"""
from typing import Optional
from pydantic import BaseModel

class {{ table_name | to_pascal_case }}(BaseModel):
    """{{ table_name | to_pascal_case }} model."""
    {% for attr in attributes %}
    {{ attr.name }}: {% if attr.type == 'N' %}int{% else %}str{% endif %}
    {% endfor %}

    class Config:
        """Model configuration."""
        from_attributes = True