"""
{{ table }} model.
"""
from typing import Optional
from pydantic import BaseModel

class {{ table }}(BaseModel):
    """{{ table}} model."""
    {% for attr in attributes %}
    {{ attr.name }}: {% if attr.type == 'N' %}int{% else %}str{% endif %}
    {% endfor %}

    class Config:
        """Model configuration."""
        from_attributes = True