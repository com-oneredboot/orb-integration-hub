/**
 * {{ table_name | to_pascal_case }} model.
 */
export interface {{ table_name | to_pascal_case }} {
  {% for attr in attributes %}
  {{ attr.name }}: {% if attr.type == 'N' %}number{% else %}string{% endif %};
  {% endfor %}
}
