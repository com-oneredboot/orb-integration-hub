# --------------------------------------------------- #
AWSTemplateFormatVersion: 2010-09-09

# --------------------------------------------------- #
Transform: AWS::Serverless-2016-10-31

# --------------------------------------------------- #
Description: >
  Application Infrastructure - AppSync Stack

# --------------------------------------------------- #
Parameters:
  Environment:
    Default: dev
    Type: String
  CustomerId:
    Default: orb
    Type: String
  ProjectId:
    Default: integration-hub
    Type: String
  TracingIs:
    Default: Active
    Type: String
  SchemaS3Key:
    Type: String
    Description: Name of the GraphQL schema file in S3

# --------------------------------------------------- #
Resources:
  # IAM Roles for AppSync
  AppSyncServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: appsync.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonDynamoDBFullAccess
        - arn:aws:iam::aws:policy/AWSLambda_FullAccess
      Policies:
        - PolicyName: SSMParameterAccess
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - ssm:GetParameter
                  - ssm:GetParameters
                Resource: !Sub 'arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/${Environment}/${CustomerId}/${ProjectId}/*'
  

  AppSyncLoggingServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: appsync.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSAppSyncPushToCloudWatchLogs


  IntegrationHubApi:
    Type: AWS::AppSync::GraphQLApi
    Properties:
      Name: !Sub '${CustomerId}-${ProjectId}-${Environment}-appsync'
      AuthenticationType: AMAZON_COGNITO_USER_POOLS
      UserPoolConfig:
        UserPoolId: !Sub "resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-cognito-user-pool-id"
        AwsRegion: !Ref AWS::Region
        DefaultAction: ALLOW
      AdditionalAuthenticationProviders:
        - AuthenticationType: API_KEY
      LogConfig:
        CloudWatchLogsRoleArn: !GetAtt AppSyncLoggingServiceRole.Arn
        FieldLogLevel: ALL
        ExcludeVerboseContent: false


  IntegrationHubApiKey:
    Type: AWS::AppSync::ApiKey
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Description: "API Key for unauthenticated access"
      Expires: 1767211838


  ApiKeySecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${CustomerId}-${ProjectId}-${Environment}-appsync-graphql-key'
      Description: "AppSync API Key for unauthenticated access"
      SecretString: !GetAtt IntegrationHubApiKey.ApiKey


  IntegrationHubSchema:
    Type: AWS::AppSync::GraphQLSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      DefinitionS3Location: !Sub 's3://${CustomerId}-${ProjectId}-build-templates/${SchemaS3Key}'

  # Data Sources
  ApplicationsDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: ApplicationsDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-applications-table-name'
        AwsRegion: !Ref AWS::Region

  # Applications Resolvers
  ApplicationsCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: applicationsCreate
      DataSourceName: !GetAtt ApplicationsDataSource.Name
      RequestMappingTemplate: |
        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)          },
          "attributeValues": $util.toJson($ctx.args.input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "message": "Applications created successfully",
          "data": $ctx.args.input
        })
        
        $util.toJson($response)


  ApplicationsQueryByIdResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: applicationsQueryById
      DataSourceName: !GetAtt ApplicationsDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "data": {}
        })
        
        #if($ctx.result)
          #if($ctx.result.items)
            #if($ctx.result.items.size() == 0)
              $util.qr($response.put("data", null))
            #else
              $util.qr($response.put("data", $ctx.result.items[0]))
            #end
          #else
            $util.qr($response.put("data", $ctx.result))
          #end
        #else
          $util.qr($response.put("data", null))
        #end
        
        $util.toJson($response)


  ApplicationsUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: applicationsUpdate
      DataSourceName: !GetAtt ApplicationsDataSource.Name
      RequestMappingTemplate: |
        #set($input = $ctx.args.input)
        #set($now = $util.time.nowISO8601())
        
        #if(!$input.application_id)
          $util.error("application_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "application_id" && $field != "None")
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "status_code": 200,
          "message": "Applications updated successfully",
          "application_id": $util.toJson($ctx.args.input.application_id)
        }
  ApplicationRolesDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: ApplicationRolesDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-application-roles-table-name'
        AwsRegion: !Ref AWS::Region

  # ApplicationRoles Resolvers
  ApplicationRolesCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: applicationRolesCreate
      DataSourceName: !GetAtt ApplicationRolesDataSource.Name
      RequestMappingTemplate: |
        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)            ,"role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)          },
          "attributeValues": $util.toJson($ctx.args.input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "message": "ApplicationRoles created successfully",
          "data": $ctx.args.input
        })
        
        $util.toJson($response)


  ApplicationRolesQueryByIdResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: applicationRolesQueryById
      DataSourceName: !GetAtt ApplicationRolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)              ,"role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "data": {}
        })
        
        #if($ctx.result)
          #if($ctx.result.items)
            #if($ctx.result.items.size() == 0)
              $util.qr($response.put("data", null))
            #else
              $util.qr($response.put("data", $ctx.result.items[0]))
            #end
          #else
            $util.qr($response.put("data", $ctx.result))
          #end
        #else
          $util.qr($response.put("data", null))
        #end
        
        $util.toJson($response)


  ApplicationRolesUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: applicationRolesUpdate
      DataSourceName: !GetAtt ApplicationRolesDataSource.Name
      RequestMappingTemplate: |
        #set($input = $ctx.args.input)
        #set($now = $util.time.nowISO8601())
        
        #if(!$input.application_id)
          $util.error("application_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "application_id" && $field != "role_id")
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)            ,"role_id": $util.dynamodb.toDynamoDBJson($input.role_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "status_code": 200,
          "message": "ApplicationRoles updated successfully",
          "application_id": $util.toJson($ctx.args.input.application_id)
        }
  ApplicationUsersDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: ApplicationUsersDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-application-users-table-name'
        AwsRegion: !Ref AWS::Region

  # ApplicationUsers Resolvers
  ApplicationUsersCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: applicationUsersCreate
      DataSourceName: !GetAtt ApplicationUsersDataSource.Name
      RequestMappingTemplate: |
        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)            ,"user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)          },
          "attributeValues": $util.toJson($ctx.args.input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "message": "ApplicationUsers created successfully",
          "data": $ctx.args.input
        })
        
        $util.toJson($response)


  ApplicationUsersQueryByIdResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: applicationUsersQueryById
      DataSourceName: !GetAtt ApplicationUsersDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)              ,"user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "data": {}
        })
        
        #if($ctx.result)
          #if($ctx.result.items)
            #if($ctx.result.items.size() == 0)
              $util.qr($response.put("data", null))
            #else
              $util.qr($response.put("data", $ctx.result.items[0]))
            #end
          #else
            $util.qr($response.put("data", $ctx.result))
          #end
        #else
          $util.qr($response.put("data", null))
        #end
        
        $util.toJson($response)


  ApplicationUsersUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: applicationUsersUpdate
      DataSourceName: !GetAtt ApplicationUsersDataSource.Name
      RequestMappingTemplate: |
        #set($input = $ctx.args.input)
        #set($now = $util.time.nowISO8601())
        
        #if(!$input.application_id)
          $util.error("application_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "application_id" && $field != "user_id")
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)            ,"user_id": $util.dynamodb.toDynamoDBJson($input.user_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "status_code": 200,
          "message": "ApplicationUsers updated successfully",
          "application_id": $util.toJson($ctx.args.input.application_id)
        }
  RolesDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: RolesDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-roles-table-name'
        AwsRegion: !Ref AWS::Region

  # Roles Resolvers
  RolesCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: rolesCreate
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)            ,"application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)          },
          "attributeValues": $util.toJson($ctx.args.input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "message": "Roles created successfully",
          "data": $ctx.args.input
        })
        
        $util.toJson($response)


  RolesQueryByIdResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: rolesQueryById
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.role_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)              ,"application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)            }
          }
        #else
          $util.error("Invalid identifier. Must provide role_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "data": {}
        })
        
        #if($ctx.result)
          #if($ctx.result.items)
            #if($ctx.result.items.size() == 0)
              $util.qr($response.put("data", null))
            #else
              $util.qr($response.put("data", $ctx.result.items[0]))
            #end
          #else
            $util.qr($response.put("data", $ctx.result))
          #end
        #else
          $util.qr($response.put("data", null))
        #end
        
        $util.toJson($response)


  RolesUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: rolesUpdate
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #set($input = $ctx.args.input)
        #set($now = $util.time.nowISO8601())
        
        #if(!$input.role_id)
          $util.error("role_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "role_id" && $field != "application_id")
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "role_id": $util.dynamodb.toDynamoDBJson($input.role_id)            ,"application_id": $util.dynamodb.toDynamoDBJson($input.application_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "status_code": 200,
          "message": "Roles updated successfully",
          "role_id": $util.toJson($ctx.args.input.role_id)
        }
  UsersDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: UsersDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-users-table-name'
        AwsRegion: !Ref AWS::Region

  # Users Resolvers
  UsersCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: usersCreate
      DataSourceName: !GetAtt UsersDataSource.Name
      RequestMappingTemplate: |
        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)          },
          "attributeValues": $util.toJson($ctx.args.input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "message": "Users created successfully",
          "data": $ctx.args.input
        })
        
        $util.toJson($response)


  UsersQueryByIdResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: usersQueryById
      DataSourceName: !GetAtt UsersDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.user_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)            }
          }
        #else
          $util.error("Invalid identifier. Must provide user_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "status_code": 200,
          "data": {}
        })
        
        #if($ctx.result)
          #if($ctx.result.items)
            #if($ctx.result.items.size() == 0)
              $util.qr($response.put("data", null))
            #else
              $util.qr($response.put("data", $ctx.result.items[0]))
            #end
          #else
            $util.qr($response.put("data", $ctx.result))
          #end
        #else
          $util.qr($response.put("data", null))
        #end
        
        $util.toJson($response)


  UsersUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: usersUpdate
      DataSourceName: !GetAtt UsersDataSource.Name
      RequestMappingTemplate: |
        #set($input = $ctx.args.input)
        #set($now = $util.time.nowISO8601())
        
        #if(!$input.user_id)
          $util.error("user_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "user_id" && $field != "None")
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "user_id": $util.dynamodb.toDynamoDBJson($input.user_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "status_code": 200,
          "message": "Users updated successfully",
          "user_id": $util.toJson($ctx.args.input.user_id)
        }
# --------------------------------------------------- #
Outputs:
  GraphQLApiUrl:
    Description: 'URL of the AppSync GraphQL API'
    Value: !GetAtt IntegrationHubApi.GraphQLUrl
  GraphQLApiKeySecretName:
    Description: 'Name of the Secret containing the AppSync API Key'
    Value: !Ref ApiKeySecret 