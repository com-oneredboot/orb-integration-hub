# --------------------------------------------------- #
AWSTemplateFormatVersion: 2010-09-09

# --------------------------------------------------- #
Transform: AWS::Serverless-2016-10-31

# --------------------------------------------------- #
Description: >
  Application Infrastructure - AppSync Stack

# --------------------------------------------------- #
Parameters:
  Environment:
    Default: dev
    Type: String
  CustomerId:
    Default: orb
    Type: String
  ProjectId:
    Default: integration-hub
    Type: String
  TracingIs:
    Default: Active
    Type: String
  SchemaS3Key:
    Type: String
    Description: Name of the GraphQL schema file in S3

# --------------------------------------------------- #
Resources:
  # IAM Roles for AppSync
  AppSyncServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: appsync.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonDynamoDBFullAccess
        - arn:aws:iam::aws:policy/AWSLambda_FullAccess
      Policies:
        - PolicyName: SSMParameterAccess
          PolicyDocument:
            Version: 2012-10-17
            Statement:
              - Effect: Allow
                Action:
                  - ssm:GetParameter
                  - ssm:GetParameters
                Resource: !Sub 'arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/${Environment}/${CustomerId}/${ProjectId}/*'
  

  AppSyncLoggingServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Principal:
              Service: appsync.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSAppSyncPushToCloudWatchLogs


  IntegrationHubApi:
    Type: AWS::AppSync::GraphQLApi
    Properties:
      Name: !Sub '${CustomerId}-${ProjectId}-${Environment}-appsync'
      AuthenticationType: AMAZON_COGNITO_USER_POOLS
      UserPoolConfig:
        UserPoolId: !Sub "resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-cognito-user-pool-id"
        AwsRegion: !Ref AWS::Region
        DefaultAction: ALLOW
      AdditionalAuthenticationProviders:
        - AuthenticationType: API_KEY
      LogConfig:
        CloudWatchLogsRoleArn: !GetAtt AppSyncLoggingServiceRole.Arn
        FieldLogLevel: ALL
        ExcludeVerboseContent: false


  IntegrationHubApiKey:
    Type: AWS::AppSync::ApiKey
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Description: "API Key for unauthenticated access"
      Expires: 1767211838


  ApiKeySecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${CustomerId}-${ProjectId}-${Environment}-appsync-graphql-key'
      Description: "AppSync API Key for unauthenticated access"
      SecretString: !GetAtt IntegrationHubApiKey.ApiKey


  IntegrationHubSchema:
    Type: AWS::AppSync::GraphQLSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      DefinitionS3Location: !Sub 's3://${CustomerId}-${ProjectId}-build-templates/${SchemaS3Key}'

  # Data Sources
  ApplicationsDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: ApplicationsDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-Applications-table-name'
        AwsRegion: !Ref AWS::Region

  # Applications Resolvers
  ApplicationsCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: ApplicationsCreate
      DataSourceName: !GetAtt ApplicationsDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)          },
          "attributeValues": $util.toJson($input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "Applications created successfully",
          "Data": $ctx.args.input
        }


  ApplicationsQueryByapplication_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationsQueryByapplication_id
      DataSourceName: !GetAtt ApplicationsDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        
        $util.toJson($response)
  ApplicationsUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: ApplicationsUpdate
      DataSourceName: !GetAtt ApplicationsDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        #if(!$input.application_id)
          $util.error("application_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "application_id" )
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "[[ schema.table ]] updated successfully",
          "Data": $ctx.args.input
        }
  ApplicationrolesDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: ApplicationrolesDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-Applicationroles-table-name'
        AwsRegion: !Ref AWS::Region

  # Applicationroles Resolvers
  ApplicationrolesCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: ApplicationrolesCreate
      DataSourceName: !GetAtt ApplicationrolesDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)            ,"role_id": $util.dynamodb.toDynamoDBJson($input.role_id)          },
          "attributeValues": $util.toJson($input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "Applicationroles created successfully",
          "Data": $ctx.args.input
        }


  ApplicationrolesQueryByapplication_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationrolesQueryByapplication_id
      DataSourceName: !GetAtt ApplicationrolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        

        $util.toJson($response)  ApplicationrolesQueryByrole_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationrolesQueryByrole_id
      DataSourceName: !GetAtt ApplicationrolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.role_id)
          {
            "version": "2017-02-28",
            "operation": "Query",
            "index": "role_id-index",
            "query": {
              "expression": "#role_id = :role_id",
              "expressionNames": {
                "#role_id": "role_id"
              },
              "expressionValues": {
                ":$role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)
              }
            }
          }
        #else
          $util.error("Invalid identifier. Must provide role_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": []
        })
        
        #if($ctx.result.items)
          #set($items = [])
          #foreach($item in $ctx.result.items)
            #set($result = {})
            #foreach($field in $item.keySet())
              #set($camelField = $util.str.toCamelCase($field))
              $util.qr($result.put($camelField, $item.get($field)))
            #end
            $util.qr($items.add($result))
          #end
          $util.qr($response.put("Data", $items))
        #end
        
        $util.toJson($response)


  ApplicationrolesQueryByBothResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationrolesQueryByBoth
      DataSourceName: !GetAtt ApplicationrolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id && $ctx.args.input.role_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
              ,"role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)
            }
          }
        #else
          $util.error("Invalid identifiers. Must provide both application_id and role_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        
        $util.toJson($response)
  ApplicationrolesUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: ApplicationrolesUpdate
      DataSourceName: !GetAtt ApplicationrolesDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        #if(!$input.application_id)
          $util.error("application_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "application_id"  && $field != "role_id" )
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)            ,"[[ schema.sort_key ]]": $util.dynamodb.toDynamoDBJson($input.[[ schema.sort_key ]])          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "[[ schema.table ]] updated successfully",
          "Data": $ctx.args.input
        }
  ApplicationusersDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: ApplicationusersDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-Applicationusers-table-name'
        AwsRegion: !Ref AWS::Region

  # Applicationusers Resolvers
  ApplicationusersCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: ApplicationusersCreate
      DataSourceName: !GetAtt ApplicationusersDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)            ,"user_id": $util.dynamodb.toDynamoDBJson($input.user_id)          },
          "attributeValues": $util.toJson($input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "Applicationusers created successfully",
          "Data": $ctx.args.input
        }


  ApplicationusersQueryByapplication_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationusersQueryByapplication_id
      DataSourceName: !GetAtt ApplicationusersDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        

        $util.toJson($response)  ApplicationusersQueryByuser_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationusersQueryByuser_id
      DataSourceName: !GetAtt ApplicationusersDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.user_id)
          {
            "version": "2017-02-28",
            "operation": "Query",
            "index": "user_id-index",
            "query": {
              "expression": "#user_id = :user_id",
              "expressionNames": {
                "#user_id": "user_id"
              },
              "expressionValues": {
                ":$user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)
              }
            }
          }
        #else
          $util.error("Invalid identifier. Must provide user_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": []
        })
        
        #if($ctx.result.items)
          #set($items = [])
          #foreach($item in $ctx.result.items)
            #set($result = {})
            #foreach($field in $item.keySet())
              #set($camelField = $util.str.toCamelCase($field))
              $util.qr($result.put($camelField, $item.get($field)))
            #end
            $util.qr($items.add($result))
          #end
          $util.qr($response.put("Data", $items))
        #end
        
        $util.toJson($response)


  ApplicationusersQueryByBothResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: ApplicationusersQueryByBoth
      DataSourceName: !GetAtt ApplicationusersDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id && $ctx.args.input.user_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
              ,"user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)
            }
          }
        #else
          $util.error("Invalid identifiers. Must provide both application_id and user_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        
        $util.toJson($response)
  ApplicationusersUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: ApplicationusersUpdate
      DataSourceName: !GetAtt ApplicationusersDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        #if(!$input.application_id)
          $util.error("application_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "application_id"  && $field != "user_id" )
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "application_id": $util.dynamodb.toDynamoDBJson($input.application_id)            ,"[[ schema.sort_key ]]": $util.dynamodb.toDynamoDBJson($input.[[ schema.sort_key ]])          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "[[ schema.table ]] updated successfully",
          "Data": $ctx.args.input
        }
  RolesDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: RolesDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-Roles-table-name'
        AwsRegion: !Ref AWS::Region

  # Roles Resolvers
  RolesCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: RolesCreate
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "role_id": $util.dynamodb.toDynamoDBJson($input.role_id)            ,"application_id": $util.dynamodb.toDynamoDBJson($input.application_id)          },
          "attributeValues": $util.toJson($input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "Roles created successfully",
          "Data": $ctx.args.input
        }


  RolesQueryByrole_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: RolesQueryByrole_id
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.role_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)
            }
          }
        #else
          $util.error("Invalid identifier. Must provide role_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        

        $util.toJson($response)  RolesQueryByapplication_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: RolesQueryByapplication_id
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "Query",
            "index": "application_id-index",
            "query": {
              "expression": "#application_id = :application_id",
              "expressionNames": {
                "#application_id": "application_id"
              },
              "expressionValues": {
                ":$application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
              }
            }
          }
        #else
          $util.error("Invalid identifier. Must provide application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": []
        })
        
        #if($ctx.result.items)
          #set($items = [])
          #foreach($item in $ctx.result.items)
            #set($result = {})
            #foreach($field in $item.keySet())
              #set($camelField = $util.str.toCamelCase($field))
              $util.qr($result.put($camelField, $item.get($field)))
            #end
            $util.qr($items.add($result))
          #end
          $util.qr($response.put("Data", $items))
        #end
        
        $util.toJson($response)


  RolesQueryByBothResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: RolesQueryByBoth
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.role_id && $ctx.args.input.application_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "role_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.role_id)
              ,"application_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.application_id)
            }
          }
        #else
          $util.error("Invalid identifiers. Must provide both role_id and application_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        
        $util.toJson($response)
  RolesUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: RolesUpdate
      DataSourceName: !GetAtt RolesDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        #if(!$input.role_id)
          $util.error("role_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "role_id"  && $field != "application_id" )
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "role_id": $util.dynamodb.toDynamoDBJson($input.role_id)            ,"[[ schema.sort_key ]]": $util.dynamodb.toDynamoDBJson($input.[[ schema.sort_key ]])          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "[[ schema.table ]] updated successfully",
          "Data": $ctx.args.input
        }
  UsersDataSource:
    Type: AWS::AppSync::DataSource
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      Name: UsersDataSource
      Type: AMAZON_DYNAMODB
      ServiceRoleArn: !GetAtt AppSyncServiceRole.Arn
      DynamoDBConfig:
        TableName: !Sub 'resolve:ssm:${CustomerId}-${ProjectId}-${Environment}-Users-table-name'
        AwsRegion: !Ref AWS::Region

  # Users Resolvers
  UsersCreateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: UsersCreate
      DataSourceName: !GetAtt UsersDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        {
          "version": "2017-02-28",
          "operation": "PutItem",
          "key": {
            "user_id": $util.dynamodb.toDynamoDBJson($input.user_id)          },
          "attributeValues": $util.toJson($input)
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "Users created successfully",
          "Data": $ctx.args.input
        }


  UsersQueryByuser_idResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Query
      FieldName: UsersQueryByuser_id
      DataSourceName: !GetAtt UsersDataSource.Name
      RequestMappingTemplate: |
        #if($ctx.args.input.user_id)
          {
            "version": "2017-02-28",
            "operation": "GetItem",
            "key": {
              "user_id": $util.dynamodb.toDynamoDBJson($ctx.args.input.user_id)
            }
          }
        #else
          $util.error("Invalid identifier. Must provide user_id")
        #end
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        #set($response = {
          "StatusCode": 200,
          "Data": {}
        })
        
        #if($ctx.result)
          #set($result = {})
          #foreach($field in $ctx.result.keySet())
            #set($camelField = $util.str.toCamelCase($field))
            $util.qr($result.put($camelField, $ctx.result.get($field)))
          #end
          $util.qr($response.put("Data", $result))
        #else
          $util.qr($response.put("Data", null))
        #end
        
        $util.toJson($response)
  UsersUpdateResolver:
    Type: AWS::AppSync::Resolver
    DependsOn: IntegrationHubSchema
    Properties:
      ApiId: !GetAtt IntegrationHubApi.ApiId
      TypeName: Mutation
      FieldName: UsersUpdate
      DataSourceName: !GetAtt UsersDataSource.Name
      RequestMappingTemplate: |
        #set($input = {})
        #foreach($field in $ctx.args.input.keySet())
          #set($dynamoField = $util.str.toSnakeCase($field))
          $util.qr($input.put($dynamoField, $ctx.args.input.get($field)))
        #end

        #if(!$input.user_id)
          $util.error("user_id is a required field for updates")
        #end
        
        #set($expNames = {})
        #set($expValues = {})
        #set($expSet = [])
        
        #set($updateCount = 0)
        #foreach($field in $input.keySet())
          #if($field != "user_id" )
            $util.qr($expNames.put("#$field", "$field"))
            $util.qr($expValues.put(":$field", $util.dynamodb.toDynamoDB($input.get($field))))
            $util.qr($expSet.add("#$field = :$field"))
            #set($updateCount = $updateCount + 1)
          #end
        #end
        
        #if($updateCount == 0)
          $util.error("No fields to update were provided")
        #end
        
        $util.qr($expNames.put("#updated_at", "updated_at"))
        $util.qr($expValues.put(":updated_at", $util.dynamodb.toDynamoDB($now)))
        $util.qr($expSet.add("#updated_at = :updated_at"))
        
        #set($expression = "SET " + $expSet.join(", "))
        
        {
          "version": "2018-05-29",
          "operation": "UpdateItem",
          "key": {
            "user_id": $util.dynamodb.toDynamoDBJson($input.user_id)          },
          "update": {
            "expression": "$expression",
            "expressionNames": $util.toJson($expNames),
            "expressionValues": $util.toJson($expValues)
          }
        }
      ResponseMappingTemplate: |
        #if($ctx.error)
          $util.error($ctx.error.message, $ctx.error.type)
        #end
        
        {
          "StatusCode": 200,
          "Message": "[[ schema.table ]] updated successfully",
          "Data": $ctx.args.input
        }
# --------------------------------------------------- #
Outputs:
  GraphQLApiUrl:
    Description: 'URL of the AppSync GraphQL API'
    Value: !GetAtt IntegrationHubApi.GraphQLUrl
  GraphQLApiKeySecretName:
    Description: 'Name of the Secret containing the AppSync API Key'
    Value: !Ref ApiKeySecret 