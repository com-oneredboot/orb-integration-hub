[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pydantic = ">=2.0.0"
boto3 = ">=1.26.0"
typing-extensions = ">=4.0.0"

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=4.0.0"
pytest-asyncio = ">=0.21.0"
moto = ">=4.0.0"
black = ">=23.0.0"
isort = ">=5.12.0"
mypy = ">=1.0.0"
pylint = ">=2.16.0"
pre-commit = ">=3.0.0"

[requires]
python_version = "3.12"