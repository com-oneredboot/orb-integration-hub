// AUTO-GENERATED by orb-schema-generator v2.0.3 - DO NOT EDIT
// Regenerate with: orb-schema generate
/**
 * Application deployment environments
 */

export enum Environment {
  /** Unknown environment */
  Unknown = "UNKNOWN",
  /** Production environment */
  Production = "PRODUCTION",
  /** Staging environment */
  Staging = "STAGING",
  /** Development environment */
  Development = "DEVELOPMENT",
  /** Test environment */
  Test = "TEST",
  /** Preview environment */
  Preview = "PREVIEW",
}

/**
 * Convert Environment to JSON string value
 */
export function environmentToJson(value: Environment): string {
  return value as string;
}

/**
 * Create Environment from JSON string value
 */
export function environmentFromJson(json: string): Environment {
  const values = Object.values(Environment);
  const found = values.find((v) => v === json);
  if (found !== undefined) {
    return found as Environment;
  }
  throw new Error(`Unknown Environment value: ${json}`);
}