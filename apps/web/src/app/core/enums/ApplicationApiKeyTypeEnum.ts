// AUTO-GENERATED by orb-schema-generator v2.0.3 - DO NOT EDIT
// Regenerate with: orb-schema generate
/**
 * Type of API key (publishable for frontend, secret for backend)
 */

export enum ApplicationApiKeyType {
  /** Unknown key type */
  Unknown = "UNKNOWN",
  /** Frontend-safe key with pk_ prefix, validated against allowed origins */
  Publishable = "PUBLISHABLE",
  /** Backend-only key with sk_ prefix, full permissions */
  Secret = "SECRET",
}

/**
 * Convert ApplicationApiKeyType to JSON string value
 */
export function applicationapikeytypeToJson(value: ApplicationApiKeyType): string {
  return value as string;
}

/**
 * Create ApplicationApiKeyType from JSON string value
 */
export function applicationapikeytypeFromJson(json: string): ApplicationApiKeyType {
  const values = Object.values(ApplicationApiKeyType);
  const found = values.find((v) => v === json);
  if (found !== undefined) {
    return found as ApplicationApiKeyType;
  }
  throw new Error(`Unknown ApplicationApiKeyType value: ${json}`);
}