// AUTO-GENERATED by orb-schema-generator v1.1.0 - DO NOT EDIT
// Regenerate with: orb-schema generate
/**
 * Recovery actions determined by the smart check flow
 */

export enum RecoveryAction {
  /** Unknown action */
  Unknown = "UNKNOWN",
  /** User needs to complete new signup */
  NewSignup = "NEW_SIGNUP",
  /** Resend email verification code */
  ResendVerification = "RESEND_VERIFICATION",
  /** Create DynamoDB record for existing Cognito user */
  CreateDynamoRecord = "CREATE_DYNAMO_RECORD",
  /** User can proceed to login */
  Login = "LOGIN",
  /** User needs to reset password */
  PasswordReset = "PASSWORD_RESET",
  /** User needs to contact support */
  ContactSupport = "CONTACT_SUPPORT",
}

/**
 * Convert RecoveryAction to JSON string value
 */
export function recoveryactionToJson(value: RecoveryAction): string {
  return value as string;
}

/**
 * Create RecoveryAction from JSON string value
 */
export function recoveryactionFromJson(json: string): RecoveryAction {
  const values = Object.values(RecoveryAction);
  const found = values.find((v) => v === json);
  if (found !== undefined) {
    return found as RecoveryAction;
  }
  throw new Error(`Unknown RecoveryAction value: ${json}`);
}