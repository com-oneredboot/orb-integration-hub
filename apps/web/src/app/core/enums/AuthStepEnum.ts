// AUTO-GENERATED by orb-schema-generator v1.3.0 - DO NOT EDIT
// Regenerate with: orb-schema generate
/**
 * Authentication flow step states
 */

export enum AuthStep {
  /** Unknown step */
  Unknown = "UNKNOWN",
  /** Initial email entry step */
  EmailEntry = "EMAIL_ENTRY",
  /** Email step (alias for EMAIL_ENTRY) */
  Email = "EMAIL",
  /** Password entry for existing users */
  Password = "PASSWORD",
  /** Password verification step */
  PasswordVerify = "PASSWORD_VERIFY",
  /** Password setup for new users */
  PasswordSetup = "PASSWORD_SETUP",
  /** Email verification code entry */
  EmailVerify = "EMAIL_VERIFY",
  /** Sign in step for verified users */
  Signin = "SIGNIN",
  /** MFA setup for users without MFA */
  MfaSetup = "MFA_SETUP",
  /** MFA code verification */
  MfaVerify = "MFA_VERIFY",
  /** Password reset initiation */
  PasswordReset = "PASSWORD_RESET",
  /** Password reset code verification */
  PasswordResetVerify = "PASSWORD_RESET_VERIFY",
  /** New password confirmation */
  PasswordResetConfirm = "PASSWORD_RESET_CONFIRM",
  /** Authentication flow complete */
  Complete = "COMPLETE",
}

/**
 * Convert AuthStep to JSON string value
 */
export function authstepToJson(value: AuthStep): string {
  return value as string;
}

/**
 * Create AuthStep from JSON string value
 */
export function authstepFromJson(json: string): AuthStep {
  const values = Object.values(AuthStep);
  const found = values.find((v) => v === json);
  if (found !== undefined) {
    return found as AuthStep;
  }
  throw new Error(`Unknown AuthStep value: ${json}`);
}