# AUTO-GENERATED by orb-schema-generator v1.1.0 - DO NOT EDIT
# Regenerate with: orb-schema generate
"""
Application API key status
"""

from enum import StrEnum


class ApplicationApiKeyStatus(StrEnum):
    """Application API key status"""

    # Unknown status
    UNKNOWN = "UNKNOWN"
    # Active API key
    ACTIVE = "ACTIVE"
    # Key is being rotated (both old and new keys valid)
    ROTATING = "ROTATING"
    # Key has been revoked and is no longer valid
    REVOKED = "REVOKED"
    # Key has expired and is no longer valid
    EXPIRED = "EXPIRED"

    def to_json(self) -> str:
        """Convert enum to JSON string value."""
        return self.value

    @classmethod
    def from_json(cls, value: str) -> "ApplicationApiKeyStatus":
        """Create enum from JSON string value."""
        for member in cls:
            if member.value == value:
                return member
        raise ValueError(f"Unknown ApplicationApiKeyStatus value: {value}")
