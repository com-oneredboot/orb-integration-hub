# AUTO-GENERATED by orb-schema-generator v2.0.3 - DO NOT EDIT
# Regenerate with: orb-schema generate
"""
Type of API key (publishable for frontend, secret for backend)
"""

from enum import StrEnum


class ApplicationApiKeyType(StrEnum):
    """Type of API key (publishable for frontend, secret for backend)"""

    # Unknown key type
    UNKNOWN = "UNKNOWN"
    # Frontend-safe key with pk_ prefix, validated against allowed origins
    PUBLISHABLE = "PUBLISHABLE"
    # Backend-only key with sk_ prefix, full permissions
    SECRET = "SECRET"

    def to_json(self) -> str:
        """Convert enum to JSON string value."""
        return self.value

    @classmethod
    def from_json(cls, value: str) -> "ApplicationApiKeyType":
        """Create enum from JSON string value."""
        for member in cls:
            if member.value == value:
                return member
        raise ValueError(f"Unknown ApplicationApiKeyType value: {value}")