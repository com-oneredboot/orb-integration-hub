[project]
name = "orb-integration-hub-api"
version = "1.3.0"
description = "Backend API for the Orb Integration Hub platform"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
